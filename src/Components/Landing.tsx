import { Link } from 'react-router-dom';
import { ScrollParallax } from 'react-just-parallax';

export default function Landing() {
	return (
		<ScrollParallax>
			<h1 className="uppercase">Nate Card</h1>
		</ScrollParallax>
	);
}
